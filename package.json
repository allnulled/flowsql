{
  "name": "@allnulled/flowsql",
  "version": "1.0.0",
  "description": "Proxy de better-sqlite3 con gestión nuclear de esquema y soporte para arrays de referencias.",
  "main": "flowsql.js",
  "scripts": {
    "build": "node dev/build-browser-node.js",
    "build:docs": "node dev/build-documentation.js",
    "dev": "npx nodemon -w flowsql-browser.tmpl.ejs -w flowsql-node.tmpl.ejs -w inc -w test-browser/index.html -e 'js,ejs' -x 'npm run test'",
    "coverage": "npx nyc --reporter=html --report-dir 'dev/coverage' npm test",
    "test": "npm run test:node",
    "test:browser": "npm run build && npx http-server -c-1 test-browser -o index.html",
    "test:node": "npm run build && node test.js"
  },
  "keywords": [
    "database",
    "db",
    "schema",
    "schema-driven",
    "arrays",
    "sqlite",
    "sqlite3"
  ],
  "author": "allnulled",
  "license": "WTFPL",
  "dependencies": {
    "@allnulled/javadoc-brute": "^1.0.4",
    "better-sqlite3": "^12.4.1",
    "body-parser": "^2.2.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "ejs": "^3.1.10",
    "javadoc": "^2.0.2"
  }
}
